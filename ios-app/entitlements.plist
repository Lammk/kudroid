<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow the app to be debugged (needed for JIT via debugger on sideload). -->
    <key>get-task-allow</key>
    <true/>

    <!-- Allow JIT compilation (required to execute PROT_EXEC pages on iOS). -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (needed for the ELF loader's RWX pages). -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow dynamic libraries to be loaded (needed for dlopen of ANGLE/MoltenVK). -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>